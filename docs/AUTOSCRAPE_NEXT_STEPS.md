# Autoscrape — Next Steps (build order)

After each step: run `npm run build`, commit, deploy and test as needed.

---

## Done
- **LLM client** — `lib/autoscrape/llm-client.ts` (OpenAI default, Claude fallback); `docs/AUTOSCRAPE_LLM.md`

---

## 1. Package structure (Python)
Restructure `scraper/autoscrape/` into:
```
scraper/autoscrape/
├── __init__.py
├── analyzer/
│   ├── __init__.py
│   ├── observer.js
│   └── session_recorder.py
├── compiler/
│   ├── __init__.py
│   ├── schema_compiler.py
│   └── schema_validator.py
└── runtime/
    ├── __init__.py
    ├── executor.py
    └── modal_runner.py
```
- Move files from flat `scraper/autoscrape/` into subdirs; fix imports (e.g. `from .analyzer.session_recorder`).
- Update `analyze.py` (or CLI entry) to use new paths. No change to Next.js build.

---

## 2. Supabase migration
- New migration (e.g. `021_autoscrape_scraped_documents.sql`):
  - Create table `scraped_documents` (id, workflow_id, run_id, case_id, doc_id, source_url, metadata jsonb, raw_text, pdf_paths text[], created_at).
  - Add columns `workflow_id`, `run_id` (nullable) to `pdf_documents`.
- No change to JS build.

---

## 3. Condition evaluator (Python)
- Add `scraper/autoscrape/runtime/condition_evaluator.py`: `evaluate(condition, row_element, context) -> (passed, captured_value)`.
- Support condition types: element_exists, element_text_matches, row_label_matches, attribute_matches, captured_field_matches, element_count_gt.
- No change to JS build.

---

## 4. Executor DB wiring (Python)
- In executor, replace in-memory DB with Supabase (supabase-py).
- Dual-write: `scraped_documents` + `pdf_documents` (for RAG); stamp every write with `run_id` (passed in from caller).
- No change to JS build.

---

## 5. Compile API route (TypeScript)
- **Next.js:** `app/api/admin/scrape/compile/route.ts` — POST, accept session JSON + optional context + captureRules + llm_provider; condense session (or accept pre-condensed); call `complete()` from `lib/autoscrape/llm-client`; merge captureRules into schema; return schema + `providerUsed` + `fallbackOccurred`.
- **Cloudflare:** `functions/api/admin/scrape/compile.ts` — same behavior, env from `context.env`.
- Protect with same admin auth as other scrape APIs. Run build after adding.

---

## 6. Modal autoscrape app
- Ensure `modal_runner.py` (or equivalent) is the autoscrape executor; accepts `schema`, `params`, `run_id`; run_id generated by caller (Worker), never by Modal.
- **Deploy (from repo root):** `modal deploy scraper/autoscrape/modal_runner.py`. Requires secret `db-credentials` in Modal; optional `anthropic-api-key` (create and attach in dashboard for Claude). After deploy, set `MODAL_AUTOSCRAPE_URL` (server-side) for “Run on Modal.”

---

## 7. /admin/autoscrape UI
- New route `/admin/autoscrape` (or tab under scrape).
- Sections: Upload session → Define capture conditions (manual rules v1) → Compile (provider dropdown, context) → Run (params, Run on Modal, poll by run_id).
- Link from existing scraper page.

---

## 8. E2E test
- Run real workflow; confirm conditional nulls, dual DB writes, run_id tracking, PDFs in storage.

---

## Build / deploy checklist
- `npm run build` must pass after any TS change.
- Python under `scraper/autoscrape/` is not part of the Pages build; only Modal and local CLI use it.
- Env: `OPENAI_API_KEY` (compile); `ANTHROPIC_API_KEY` (optional); `MODAL_AUTOSCRAPE_URL` (server-side, when Modal run is added).
